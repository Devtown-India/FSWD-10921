
const Navbar = ({ }) => {
    return (
        <nav>
            <a href="#">Calculator</a>
            <a href="#">Timer</a>
            <a href="#">Tap</a>
        </nav>
    );
}

export default Navbar;